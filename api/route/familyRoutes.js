const express = require('express');
const router = express.Router();
// const { getAllMembers, createMember } = require('../service/familyService');
// const { validateFamilyMember } = require('../validator/familyValidator');

// Get all family members
// router.get('/', getAllMembers);

// Create a new family member
// router.post('/', validateFamilyMember, createMember);

module.exports = router;
